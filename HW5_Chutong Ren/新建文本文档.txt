V0=Vrest 100*1;
am 之类的全部改成100*6的向量每次更新
rates 做成一次性100*3矩阵每次更新
最后把INA IK IL写成三个向量，每个100*length
I-ions 写成一个100*3向量，一次性
多加一个I-ionsum 把电流加起来，也就是总电流，100*1
得到 Iion 应该也是一个100*1的向量
在计算Vnext，一个新的100*1向量
原来用显式欧拉计算Vn+1的时候。用的Iion都是这个时候的数据，但是现在用隐式，所以必须用下一个时刻的Iion，当然在计算Iion的currrent
里面用到了Vn，而Vn的计算又需要用到Iion（n+1），所以为了避免矛盾，允许用当前的V来计算Iion，也就是curreny直接只用last V ，就像上次讨论的时候出错的那样。
反正i用的是上一个，不妨每次先调用current计算iion（n+1），再计算V


V1=rest I1直接求不迭代  I2 V2 I3 V3